# Search BOLD for taxonomy data by BOLD ID.

Search BOLD for taxonomy data by BOLD ID.

## Usage

``` r
bold_tax_id2(
  id,
  dataTypes = "basic",
  includeTree = FALSE,
  response = FALSE,
  ...
)
```
